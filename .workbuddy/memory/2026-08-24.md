# 2026-08-24 工作日志

## Power BI Modeling MCP 项目内固化方案（已搁置）
- 调研 MCP 引用方式：配置在 `~/.workbuddy/mcp.json`，用 `npx -y @microsoft/powerbi-modeling-mcp@latest --start`；21 个 deferred tool，命名 `mcp__powerbi-modeling-mcp__{操作类型}`，需先 `ToolSearch` 加载 schema 再 `DeferExecuteTool` 调用。
- npx 缓存路径：`C:\Users\Administrator\AppData\Local\npm-cache\_npx\deea81b821a9ed55\node_modules\@microsoft\powerbi-modeling-mcp`（非稳定，随 @latest 变化）。
- 尝试项目内固化：项目根建 `package.json` 锁定 `@0.5.0-beta.12`，managed node 22.22.2 跑 `npm install --no-audit --no-fund` 装到 `mystyle\node_modules\`，验证主包 + win32-x64 平台包 + bin `index.js` 就绪。
- 发现问题：mcp.json 仍用 `npx @latest`，项目内 node_modules 未被引用；当前 registry/npx缓存/项目内三处版本均为 `0.5.0-beta.12`（巧合），npx 会自动追新，项目内锁版本需手改 mcp.json 切到项目内 bin 才真正生效。
- 用户决定：暂不切换，卸载项目内 package.json/package-lock.json/node_modules，保留全局 npx 方式，后续再说。
- 现状：项目根已无 npm 产物（Glob 确认）；全局 `~/.workbuddy/mcp.json` 保持 `npx @latest` 不变，MCP 工具照常可用。

## 主题修改 + venv 重建 + 脚本路径修正
- 用户基于两张 PBI Desktop 截图要求改 `theme\PBI-Style-深蓝暗色-优化.json` 的 pageNavigator：①"显示隐藏页"默认关闭 ②"形状"圆角改 6。
- 对照 `theme\reportThemeSchema-2.143.json` 定位 property：`showHiddenPages`（boolean，title "Show hidden pages"，line 6573）+ `rectangleRoundedCurve`（integer，title "Rounded Corners"，line 6675），均在 pageNavigator `*`→`*` 数组 item 内。
- 修改 pageNavigator `*` item：加 `"showHiddenPages": false`、`"rectangleRoundedCurve": 6`（首次 Edit 误引入多余 `{` 致 JSON 解析失败，已修正）。
- **重大结构修正**：所有主题/schema 文件实际在 `mystyle\theme\` 子目录（不在根，之前记忆有误）；HTML 在 `mystyle\html\`。
- **venv 重建**：git clone 后 `mystyle\.venv` 不存在（被 .gitignore 忽略），用 managed python 3.13.12 在项目内重建：`python -m venv .venv` + `pip install -r requirements.txt`（装 jsonschema==4.26.0）。
- **修 validate_theme.py 路径 bug**：原 SCHEMA/DEFAULT_THEME 指向 `mystyle\` 根（找不到文件），已改为 `HERE / "theme" / ...` 指向 `theme\` 子目录；注释用法示例同步更新。
- 校验结果：4 个 PBI 主题（浅色/浅蓝FineReport/深蓝暗色/深蓝暗色-优化）全部通过 schema 2.143 校验，错误数 = 0。
- 用户约定：后续设计主题视图 JSON 都必须参考 `theme\reportThemeSchema-2.143.json`，已记入 MEMORY.md「主题设计约定」。

## pageNavigator 按钮圆角层级修正（关键纠错）
- 用户反馈：`rectangleRoundedCurve` 写在 pageNavigator `*` item 里不生效（按钮没圆角）。
- 用户纠正：这个"形状→圆角"是 pageNavigator 专属、应该在 `pageNavigator → shape` card，不是通用装饰。
- 重新查 schema：pageNavigator 定义（6470-7569）内有 `shape` card（line 7272），含 `tileShape`（形状枚举：rectangle/rectangleRounded/rectangleRoundedByPixel/pill/...）+ `rectangleRoundedCurve`（圆角度数）。
- **正确写法**：pageNavigator 加 `shape` card，设 `tileShape: "rectangleRoundedByPixel"` + `rectangleRoundedCurve: 6`；只写 `rectangleRoundedCurve` 不设 `tileShape` 不生效。
- 已改 `theme\PBI-Style-深蓝暗色-优化.json`：pageNavigator 加 `shape` card，移除 `*` item 里错位的 `rectangleRoundedCurve`；校验通过（错误数 0）。
- **教训**：之前误判"rectangleRoundedCurve 引擎不读"——实际是层级写错。schema 里同一 property 名可能出现在多个层级（`*` item 和 `shape` card 都有），必须确认该 property 属于哪个 card。schema 校验通过 ≠ 层级对。
- MCP 能力边界澄清：用户问 MCP 能否读内存中 pbix 的 visual 配置——不能，MCP 连 Analysis Services 只管 model，visual 格式在 Report 引擎不暴露。
- RS 版限制：用户 PBIRS 2.143（2025年5月）大概率不支持 .pbip，反查 visual 格式只能走"手动设置+截图反推"。

## pageNavigator 按钮圆角：最终放弃（疑似 RS bug）
- 改到 `shape` card（`tileShape: "rectangleRoundedByPixel"` + `rectangleRoundedCurve: 6`）后**仍不生效**——重新导入主题 + 重置视觉对象格式后按钮仍无圆角。
- 判断：RS 版 2.143 的 pageNavigator `shape` card 主题控制疑似未实现或 bug。
- 用户决定先放弃，改用 accentBar + outline 模拟圆角观感。
- 当前 `PBI-Style-深蓝暗色-优化.json` 的 pageNavigator `shape` card 配置保留（语法对、校验通过、不影响其他），待后续 RS 版本验证。
- 教训沉淀到 MEMORY.md「主题 property 验证方法论」：schema 校验通过 ≠ 层级对 ≠ 运行时生效。

## MCP 实测：连接 5.营收概况.pbix 读模型（成功）
- `ListLocalInstances` 找到端口 60473 → `Connect` 成功（connection: PBIDesktop-5.营收概况-60473）。
- 读取结果：1 模型 / 58 度量值（全在"度量值"表）/ 14 关系（8 业务 + 6 LocalDateTable 自动日期噪音）。
- 度量按业务线：智趣 19 / 猿推库 12 / MCN 12 / 星图 6 / 通用 9。
- 关系：4 业务表（猿推库团长订单/智趣团长订单/星图提报管理/MCN）各连日期表-结算 + 日期表-支付。
- 实测验证 MCP model 级读写完全可用（visual 格式读不到，边界确认）。
- 发现优化点：①LocalDateTable 自动日期应关 ②58 度量无 displayFolder 分类 ③_格式化显示/-非零 衍生度量可精简 ④星图激活日期连两个日期表有歧义。

## HTML 关系图优化
- `html\5营收概况_模型关系图.html` 原是纯 SVG fragment（无外壳），下载打开白底浅字看不清。
- 优化：包完整 HTML + 项目深蓝暗色背景 #0A1428 + 节点主题色填充 + 浅色文字，单独打开也好看。

## 讨论（未执行，待用户决定）
1. 数据分析思路：多渠道营收对比/趋势/退款/预估vs实际/达人分层/毛利/直播拆分/结算支付口径差异。
2. 日期表方案：当前两个日期表（结算/支付），业界标准是一个日期表+inactive关系+USERELATIONSHIP；取决于用户用法（一次一口径→合并，同时看两口径→保留）。
3. displayFolder：按口径分（结算/支付/通用）或业务线+口径双层；重复度量候选删：智趣服务费收入2、_格式化显示系列(可用formatString替代)、-非零系列(需确认报表引用)。

## 执行：ExportTMDL + 删 11 个度量
- `ExportToTmdlFolder` 成功导出到 `mystyle\model\5营收概况\`（19 文件，含 relationships.tmdl / 度量值.tmdl / 各表）。
- 读 relationships.tmdl + 度量值.tmdl 全文，确认：度量都不显式引用日期表（靠 active 关系传递筛选）；日期表-结算用"结算日期"列、日期表-支付用"支付日期"列关联。
- Grep 确认 `_格式化显示`(5) + `-非零`(6) 共 11 个在 model 内无其他度量引用。
- `measure_operations.Delete` 批量删 11 个（useTransaction 原子性），成功：度量 58 → 47。
- ⚠️ 智趣服务费收入2（与 智趣-服务费收入 表达式完全相同）仍保留，用户未授权删，待确认。

## 改关系方案（待用户确认 trade-off）
- 用户想合并成一个日期表。但 trade-off：合并后每个跨口径业务度量要写两版（结算口径默认 + 支付口径 USERELATIONSHIP），度量从 ~30 翻倍到 ~60。
- 当前"2日期表+30度量（不区分口径，用户选日期表决定）" vs 合并后"1日期表+60度量（区分口径）"。
- 我的立场：总览报表常需同页两口径对比，保留两个日期表更划算；若用户仍要合并，给出执行方案（保留日期表-结算改名+加统一日期列+支付关系 inactive+25个度量写支付版）。

## 执行：方案A + 删关系 + displayFolder（部分失败）
- 用户定方案A（保留两个日期表，只加 displayFolder 分类）+ 删 6 条 LocalDateTable 关系 + 删 智趣服务费收入2。
- ✅ 删 智趣服务费收入2 成功：度量 47 → 46（累计删 12 个：11 个 _格式化显示/-非零 + 智趣服务费收入2）。
- ❌ 删 6 条 LocalDateTable 关系失败（事务回滚）：错误"列'提报日期'中的变体对象引用的 relationship 已遭删除"——LocalDateTable 关系被列的变体（自动日期配置）引用，硬删会破坏。**正解是在 PBI Desktop 关闭"自动日期/时间"功能，LocalDateTable 会自动消失**。
- ❌ Update displayFolder 46 个全部失败：错误"为事务请求影响时，基础版本不得为负"——PBI Desktop 版本同步问题，删 12 个度量后模型版本未同步，新修改被拒。
- 待办：①用户在 PBI Desktop 应用/同步外部工具更改（可能有提示弹窗）后，重试 displayFolder Update。②LocalDateTable 关系让用户在 PBI Desktop 关"自动日期/时间"清理。
- displayFolder 分类方案（按业务线，不分口径子层）：通用(7) / 智趣(15) / 猿推库(10) / MCN(10) / 星图(4)。

## displayFolder 重试成功（断开重连刷新对象图）
- 用户在 PBI Desktop 同步外部更改 + 关"自动日期/时间"后，首次重试仍失败：`Model object-map is not consistent with the metadata-object graph`（MCP 连接缓存的对象图与 PBI 实际元数据不一致，因关自动日期导致结构变化）。
- **解决：Disconnect → ListLocalInstances → Connect 重新连接**，刷新对象图后 Update 46 个 displayFolder **全部成功**。
- 抽样 Get 确认：支付金额→通用 / 智趣-支付金额→智趣 / MCN-结算佣金→MCN，均生效（modifiedTime 2026-08-24T07:10:58）。
- **教训沉淀**：MCP 写操作失败如果是"对象图不一致"类错误，断开重连刷新对象图通常能解决；PBI Desktop 外部更改（如关自动日期）会改模型结构，必须重连同步。

## 最终状态
- 度量：58 → 46（删 12：11个_格式化显示/-非零 + 智趣服务费收入2）。
- displayFolder：46 个按 通用/智趣/猿推库/MCN/星图 分类完成。
- LocalDateTable：用户关"自动日期/时间"自动清理（6 表 + 6 关系消失），无需 MCP 删。
- 关系：保留两个日期表（结算/支付），8 条业务关系不变。

## 结算收入页面设计 + HTML 效果图
- 用户要求：针对"结算收入"页面，分析需度量+图表，做 HTML 效果图（#0A1428 背景，布局：顶栏(标题+页面导航器+日期筛选器)+KPI栏目+内容图表）。
- 设计分析：6 KPI（总营收/服务费/MCN佣金/星图激活/订单数/达人数）+ 6 图表（营收趋势折线/渠道占比环形/渠道对比条形/退款分析柱/预估vs实际对比/毛利结构指标环）。
- 已生成 `html\结算收入_效果图.html`：项目深蓝暗色主题，顶栏(营收概况+4页导航药丸+日期筛选)+6 KPI 卡+6 SVG 图表，示意数据。
- 关于"数据分析可视化智能"：workbuddy 有 show_widget 可视化 + 100+ 专家，但无通用指南替代业务判断；直接用项目设计 token + 业务分析做原型。

## 卡片背景色 4 种风格对比 HTML
- 用户问只给画布 #0A1428 时卡片色怎么选，我答标准 #142436（同色系提亮一档），并给 4 种 PBI 纯色备选。
- 用户要 4 种各做 HTML：用脚本基于"结算收入_效果图.html"批量替换 #142436 生成：
  - `结算收入_强层次.html`（#1A2D44，更跳）
  - `结算收入_偏紫科技.html`（#161428，差异化潜力）
  - `结算收入_偏青冷峻.html`（#0E1E24，冷峻）
  - `结算收入_极简暗调.html`（#0E1420，克制）
- 画布统一 #0A1428，布局一致，方便横向对比卡片色。
- 我的立场：PBI 交付场景仍推荐标准 #142436（和整套色板梯度配套）；差异化首选偏紫科技 #161428，但要确认客户吃这种调性。

## 结算收入效果图加 TOP10 商品收入图块
- 用户定卡片色 #142436（标准），并要求加"4种业务商品收入TOP10"。
- Grep TMDL 确认 4 业务表都有商品维度：智趣/猿推库有商品ID+商品名称+展示名称(=名称&ID)；MCN 有商品ID+商品标题+展示名称；星图有品牌名称（达人维度，无商品列）。
- 在 `html\结算收入_效果图.html` 加 TOP10 图块（grid-column:1/-1 跨整行，10 商品横向条形排名，色块标注业务线：智趣蓝/猿推库紫/MCN绿/星图橙），示意数据。
- 现效果图：顶栏+6KPI+6图表+1 TOP10宽图块，7 个内容区。
