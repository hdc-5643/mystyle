# 2026-08-23 工作日志

## DateRangeSlicer 七项格式与交互优化（已编译）
基于用户确认的 7 条需求实现并重新编译 `DateRangeSlicer20260822001.1.0.0.0.pbiviz`：
1. 标头背景色默认 `transparent`（不填充），格式面板"背景色"色板保留可改。
2. 标头文本位置新增枚举 `position`（top/left），默认 `top`（标题在上、输入框在下，左对齐）；标题字号默认 14、最小 8（面板+readSettings 双重 clamp）。
3. 切片器设置-边框粗细最小 1（面板 min=1 + readSettings clamp(1,100)）。
4. 圆角默认 6、最小 0、最大 10（面板 min=0 max=10 + readSettings clamp(0,10)）。
5. 值-字体大小默认 13、最小 8（面板 min=8 + readSettings clamp(8,100)）。
6. **保留系统日历弹窗**（input type=date），隐藏日历图标并让点击输入框任意位置弹窗（CSS indicator 透明热区覆盖）；min/max 属性限制可选范围；显示真实日期值（中文环境 2022/9/9 形式，无占位文本）。
   - 注意：用户最初在 AskUserQuestion 误选"强制 yyyy/m/d=自写日历组件"，随后澄清不要自写日历，改回保留系统弹窗。
7. 选择逻辑维持三态：只填开始→`>= start`；只填结束→`< end+1`；两者→`AND`；都空→无筛选。

源码改动文件：`capabilities.json`（加 header.position 枚举）、`src/visual.ts`（DEFAULTS/构造函数包裹 inputsEl/readSettings/applyStyles/格式面板）、`style/dateRangeSlicer.less`（top/left 布局 + 隐藏图标热区）。
验证建议：删除旧版自定义视觉→导入新版→标头默认上方透明背景；格式面板切换位置 top/left、调字号/边框/圆角验证边界；点击输入框弹系统日历，选值不越界；对比三态筛选提示。

## DateRangeSlicer 样式与筛选边界修复（01:01 已编译）
用户反馈当前输入框样式与顶部导航版 HTML 不一致，且结束框 `< T+1` 导致边界日期（如 4/1）被误包含。修复如下：
1. 输入框样式对齐 HTML 参考：背景 `--drs-bg=#0A1428`、字号 11px、圆角 4px、`padding: 6px 12px`、箭头色 `#888780`；标题字号默认 11px。
2. 标头 `position` 默认从 `top` 改回 `left`（标题在左、输入框在右），与截图/HTML 一致。
3. 结束框筛选逻辑从 `LessThan` 下一天零点改回 `LessThanOrEqual` 当天 `23:59:59.999Z`；开始框仍为 `GreaterThanOrEqual` 当天零点。
4. `restoreFilter` 同步移除对 `LessThan` 的 `-1 天` 回显处理。
源码改动：`src/visual.ts`（DEFAULTS、applyFilter、restoreFilter）、`style/dateRangeSlicer.less`（颜色/字号/圆角/padding/箭头）。
验证建议：导入后确认标头默认左侧；输入框视觉与 HTML 参考接近；选择 2026/3/1~2026/3/31 后，右下角筛选提示应为 `>= 2026/3/1` 且 `<= 2026/3/31`，表格不应出现 4/1 数据。

## DateRangeSlicer 初始自动下发筛选（01:10 已编译）
用户截图显示输入框有值（2023/01/01~2023/01/31）但图表仍出现 1月2日、2月1日 等范围外数据，判断为"初始只填值、不下发筛选"导致。按用户选择改回初始自动下发：
- `update()` 首次加载填充 min/max 后立即调用 `applyFilter()`，拖入字段后图表立刻受限。
- 外部"清除所有筛选器"后仍通过 `lastFilterPresent` 检测重置为 min/max。
源码改动：`src/visual.ts`（update 初始化分支）。
验证建议：删除旧版→导入新版→拖入日期字段后，图表应立即只显示 min~max 范围内数据；右下角筛选提示应出现 `>= 开始日期` 且 `<= 结束日期`。

## DateRangeSlicer 时区边界修复（01:22 已编译）
用户截图显示：选择结束日期 2026/3/31，但右下角筛选提示变成"在 4月1日 当天或之前"，且 4/1 数据被包含。根因是 `applyFilter` 用 `toDateInput(endDate) + "T23:59:59.999Z"` 构造 UTC 结束边界，该 UTC 时刻在中国时区（+8）已是 4/1 07:59，Power BI 按本地时间显示并筛选导致跨天。
修复：
1. `parseInputDate` 改为按本地时区午夜解析（`new Date(y, mo, d)`），与 `input[type=date]` 语义一致。
2. `applyFilter` 开始条件使用本地午夜 `new Date(y, mo, d)`；结束条件使用本地当天 23:59:59.999 `new Date(y, mo, d, 23, 59, 59, 999)`；序列化后 Power BI 按本地时区显示为当天开始/结束，不再跨天。
3. `parseDate` 对完整 ISO 日期时间字符串（含 Z/偏移）改为直接 `new Date(v)` 解析，确保 `restoreFilter` 切页恢复时回显日期与选择一致。
源码改动：`src/visual.ts`（parseDate、parseInputDate、applyFilter）。
验证建议：导入新版→选择 2022/9/9~2026/3/31→右下角筛选提示应为"在 9月9日 当天或之后 和 在 3月31日 当天或之前"；表格中 4/1 数据应被排除。

## DateRangeSlicer 自定义占位文本（方案C，已编译）
用户选「方案C：自定义占位文本」替换清空后的浏览器原生 `yyyy/mm/日` 格式提示。关键点：`input[type=date]` 不支持 `placeholder` 属性，原生格式提示设 placeholder 无效，故改用叠加层方案。
- 构造函数：每个 input 外包 `.drs-input-wrap`，内部再加 `.drs-placeholder` span（开始="开始日期"、结束="结束日期"）。
- 新增 `updateEmptyState()`：根据 input value 是否为空切换 wrapper 的 `is-empty` 类；在构造函数、change 监听、restoreFilter 后、resetToRange 后、初始化填充后均调用。
- CSS：`is-empty` 时显示 `.drs-placeholder`（极弱色 #5F5E5A、pointer-events:none 让点击穿透到输入框）、并把 `::-webkit-datetime-edit` 设为透明以隐藏原生格式提示；`.drs-input` 由 flex 改为 `width:100%` 填满 wrapper。
- 数据截断场景：结束框为空时占位文本改为"数据过多，请手动输入"（原 placeholder 属性本就无效，现由叠加层实现）。
源码改动：`src/visual.ts`（构造函数、update、updateEmptyState）、`style/dateRangeSlicer.less`（wrapper/placeholder/is-empty）。
验证建议：导入后清空任一日期框→应显示"开始日期"/"结束日期"而非 `yyyy/mm/日`；填入日期后占位文本消失。

## DateRangeSlicer 输入框不允许留空（自动恢复边界，已编译）
用户反馈两个关联问题：①点结束框原生「清除」后显示占位文本而非具体最大日期；②最小日期在日历里似乎无法选择。根本原因是允许输入框留空导致边界状态混乱。
改为「始终有边界」模型：
- `change` 事件处理：检测到开始/结束框为空且已有 `rangeMin`/`rangeMax` 时，自动恢复为数据最小/最大日期，再 `applyFilter`；不会出现占位文本，也不会变成单边开放筛选。
- `update()` 外部清除分支：resetToRange 后调用 `applyFilter()`，保证"清除所有筛选器"后输入框和实际筛选都回到 min~max，而不是输入框有值却未筛选。
- 简化初始化：去掉截断场景的"数据过多"占位提示，统一初始化为 min/max，避免与"不允许留空"冲突。
源码改动：`src/visual.ts`（onChange 处理、update 初始化与外部清除分支）。
验证建议：导入新版→点结束框原生"清除"→应立刻恢复为 max 日期（如 2026/03/31），筛选提示保持双边；打开开始日历→min 日期可选且已高亮；"清除所有筛选器"后图表仍受 min~max 限制。

## DateRangeSlicer 结束条件改为 LessThan（01:43 已编译）
用户希望筛选提示显示为「在 4月1日 之前」而非「在 3月31日 当天或之前」。
源码改动 `src/visual.ts`：
- `applyFilter` 结束条件由 `LessThanOrEqual`+本地当天 23:59:59.999 改为 `LessThan`+本地**次日零点**（语义等价，仅提示语变化）。
- `restoreFilter` 支持 `LessThan`：解析到次日零点后减一天回显为实际结束日期；兼容旧 `LessThanOrEqual` 格式（不减）。
筛选实际区间不变：含结束日期当天、排除次日全天。产物已重新编译覆盖。

## 待整改方案（Pending，截至 2026-08-23 02:03）
已编译项见上。以下项尚未实施，待用户核实后执行：

【布局与尺寸】
1. **整体长宽对齐 HTML 横条（~240×31px）**：`.drs-input` 固定宽 ~90px + `flex:0 0 auto`（解决 input[type=date] 最小渲染宽度偏宽、两框不等宽）；容器 `nowrap` 一行排满（标签+框+箭头+框）；header `position:left` 模式与输入框同高 ~31px。
2. **日期显示格式改为 yyyy/m/d（2026/3/1）**：复用方案C叠加层——`::-webkit-datetime-edit` 常驻透明 + 自定义显示 span；新增 `toDisplayDate(d)` 输出 `YYYY/M/D`（去前导零）；change/restoreFilter/初始化/resetToRange 同步；保留原生日历与 min/max。
3. **输入框字号偏小（与项2合并）**：叠加层 span 显式设 `font-size: var(--drs-label-size)`，不再依赖 `::-webkit-datetime-edit` 的 UA 默认字号。

【颜色与样式】
4. **值文字色改为白色**：`DEFAULTS.labels.fontColor` 由 `#B4B2A9` → `#FFFFFF`（运行时被 `setProperty('--drs-fg', l.fontColor)` 覆盖为灰色，LESS 的 #FFFFFF 仅 fallback；HTML `.date-input` color 为 #FFFFFF，需对齐）。位置：src/visual.ts 第41行。
5. **切片器圆角默认改为 3**：`DEFAULTS.selection.borderRadius` 由 6 → 3（格式面板 min=0/max=10 不变）。位置：src/visual.ts 第37行。

【可选】
6. **line-height 对齐**：`.drs-input` 当前 `line-height:1`，HTML 为 `1.5`，影响密度不影响字形，可酌情调整。

纠正前文误判：值文字色并非已为白色——`DEFAULTS.labels.fontColor` 实际是 `#B4B2A9`，运行时被覆盖为灰色，故项4确属待改。

## 7. 单边筛选（未激活侧显示边界值但不参与）
- **初始默认**：两边都未激活，start 框显示 rangeMin、end 框显示 rangeMax 作为提示值，实际筛选=无限制（全部数据）。
- **激活**：用户在任一侧日历选日期→该侧参与（>= 开始 / < 结束+1）；清空某侧→回到未激活、显示对应边界值、不参与。
- **两边清空**：input 均为空→`applyFilter` 跳过两侧→无筛选（无限制），框仍显示 min/max 提示值。
- **实现**：回退此前「不允许留空自动恢复 min/max」逻辑；复用方案C叠加层——未激活（input 为空）时叠加层显示具体边界日期（如 2022/9/9 / 2026/3/31），点击仍可弹日历选择；`applyFilter` 空值侧直接跳过。
- **视觉**：未激活侧不做弱化样式（用户明确不需要），外观与激活一致，仅逻辑不生效。

## DateRangeSlicer 全部整改项执行完成（02:21 已编译）
用户确认"都执行"，将上方 Pending 清单 ①②③④⑤⑥⑦ 全部落地并重新编译 `DateRangeSlicer20260822001.1.0.0.0.pbiviz`：
1. **①整体长宽对齐 HTML 横条**：`.drs-input`/`.drs-input-wrap` 改为固定宽 `92px` + `flex:0 0 auto`（解决 input[type=date] 原生最小渲染宽度偏宽、两框不等宽）；`.drs-inputs` 加 `flex-wrap:nowrap; white-space:nowrap` 强制「标签+框+箭头+框」一行排满；`.drs-header` 高度 28→30px 与输入框同高。
2. **②③日期格式 yyyy/m/d + 字号偏小**：废弃方案C占位层（`.drs-placeholder`），改为常驻叠加显示层 `.drs-value`——原生 `::-webkit-datetime-edit` 及 `-fields-wrapper` 常驻 `color:transparent`，唯一可见文本由 `.drs-value` 提供；新增 `toDisplayDate(d)` 输出 `YYYY/M/D`（月日去前导零），叠加层 `font-size: var(--drs-label-size)` 显式对齐，字体偏小问题一并解决。
3. **④值文字色改白**：`DEFAULTS.labels.fontColor` `#B4B2A9`→`#FFFFFF`，运行时 `--drs-fg` 同步为白，与 HTML `.date-input` 一致。
4. **⑤圆角默认改 3**：`DEFAULTS.selection.borderRadius` 6→3（面板 min0/max10 不变）。
5. **⑥line-height 对齐**：`.drs-input` `line-height` 1→1.5，框高约 31px 贴近 HTML。
7. **⑦单边筛选（未激活显示边界但不参与）**：回退「不允许留空自动恢复」逻辑——`onChange` 不再回填 min/max；`update()` 初始分支改为两侧皆空（无筛选）、`resetToRange` 改为清空两侧；`applyFilter` 空值侧直接跳过；`updateValueDisplay` 在 input 有值时显示已选日期、空值时显示 rangeMin/rangeMax 边界值（点击仍可弹日历）。筛选三态：`>= 开始` / `< 结束+1` / 两者 AND / 都空=无限制。
源码改动：`src/visual.ts`（DEFAULTS、字段 placeholder→value、构造函数、onChange、update 初始化/外部清除分支、updateValueDisplay+toDisplayDate 替代 updateEmptyState、resetToRange）、`style/dateRangeSlicer.less`（布局固定宽/nowrap、.drs-value 叠加层、原生文字常驻透明、line-height、header 高度）。
验证建议：导入后①整体紧凑贴近 HTML 横条；②输入框固定显示 `2026/3/1` 类格式且字号与标签一致；③④值白、圆角3px；⑤⑦初始无筛选（图表全量），选开始→单边 `>=`，选结束→单边 `<`，清空任一侧→显示边界值但不过滤，两侧清空→无限制且仍显示 min/max。

## DateRangeSlicer 标头/值字号微调 + 待定项反馈（02:30 已编译）
- 标头文本字号 `DEFAULTS.header.fontSize` 11→14；值文本字号 `DEFAULTS.labels.fontSize` 11→12。已重新编译 `DateRangeSlicer20260822001.1.0.0.0.pbiviz`。
- 待定反馈（需用户确认后再改）：
  1. **日历图标**：`input[type=date]` 原生 `::-webkit-calendar-picker-indicator` 当前 `opacity:0` 隐身但铺满输入框作点击热区（保留弹日历能力）；`display:none` 会令日历弹窗失效，故维持现状。两框中间的 `→` 是 `.drs-arrow` 文字元素，可独立改。
  2. **文字居中**：用户选 **B 水平居中**。`.drs-value` 由 `left:12px` 改为 `left:0;right:0;width:100%;text-align:center`（垂直仍靠 top:50%+translateY(-50%)，pointer-events:none 不影响点击弹日历）。已重新编译 `DateRangeSlicer20260822001.1.0.0.0.pbiviz`。

## DateRangeSlicer 日历定位 + 字号持久化修复（02:40 已编译）
- **问题 1（日历跳到最大日期）**：根因是"未激活"状态把 `input.value` 置空，浏览器日历无定位日期，于是跳到 `max` 属性所在月。修复：新增 `startActive`/`endActive` 显式激活态；未激活时 `input.value` 仍填入数据边界值（保证日历正确定位），但 `applyFilter` 依据激活态决定是否参与筛选。涉及 `constructor/onChange`、`update()` 初始化、`resetToRange()`、`restoreFilter()`、`applyFilter()`、`updateValueDisplay()`。
- **问题 2（值字号显示 10/11）**：代码 DEFAULTS 已改为 12，但 Power BI 持久化了旧实例格式值。修复：在 `readSettings` 增加 `migrateFontSize`——若持久化值等于旧默认值 11，则自动过渡为新默认值（header 11→14，labels 11→12）；LESS fallback `--drs-label-size` 也改为 12px。若仍显示 10，说明是用户在格式面板手动设置过 10，需在面板"值"卡中改回 12。
验证建议：导入后①点最小值框的日历应跳到 2022/09；点最大值框应跳到 2026/03。②值字号在面板"值"卡中应为 12；若之前手动设过 10，需手动调回 12 或删除旧视觉重新拖入新版。

## 经验固化：pbiviz 开发 skill（10:54）
- 将本次 DateRangeSlicer 全周期踩坑经验固化为项目级 skill：`营收概况\.workbuddy\skills\pbi-custom-visual-dev\SKILL.md`（**刻意放在项目空间而非用户级**，因内容含项目专属路径与 GUID）。
- 覆盖：input[type=date] 五大原生行为坑（placeholder无效/图标删不掉/字号不继承/强制 yyyy/m/d 格式/空值导致日历定位错位）、applyJsonFilter+AdvancedFilter 筛选逻辑与时区、Formatting Model 持久化覆盖默认值需用 migrateFontSize 迁移、深色主题取色与 HTML 顶栏对齐、调试技巧（labelEl.title tooltip）。
- 关键结论：**未激活态别用 input 空值隐式推断，用显式激活标志位**；改 DEFAULTS 必须配套迁移旧实例持久化值。

## 经验纠偏与沉淀：PBI 主题设计方法论（11:00）
- 用户指出此前对"PBI 主题设计经验"的总结方向错误——不应总结为 HTML 参考对齐，而应总结 `PBI-Style-深蓝暗色.json` **主题文件本身的底层设计逻辑**（HTML 顶栏只是可选对齐基准，不参考也能成立）。
- 已固化为项目级 skill：`营收概况\.workbuddy\skills\pbi-theme-design\SKILL.md`。
- 主题被接受的核心逻辑（6 点）：①分层色彩阶梯（页面底#0A1428→顶栏#0F1B30→卡片#142436→高亮#1A2D44→分隔#1E3A5F→边框#2C4A6B）；②dataColors 8 色按"品牌主辅(1-3)+金融语义(4涨绿5跌橙)+灰度收尾(6-8)"语义排序；③文字三级灰度(白/次级灰#B4B2A9/极弱#5F5E5A)；④统一圆角(全局 radius 8)+边框纪律；⑤导航器四态机(default/hover/selected/disabled)且 selected 用强调色收口；⑥title 默认关、网格线虚线#1E3A5F、labels 透明底白字。
- 派生新主题方法论：换基底阶梯+换品牌主辅色，保留结构(radius/border.show/title.show:false/gridlineStyle)、文字三级与导航器状态机不变，再跑 validate_theme.py 校验。

## DateRangeSlicer "未激活 value 留空" 修复（11:30 已编译）
- **问题**：未激活时把 `input.value` 填成边界值（如 min），导致日历打开时已高亮该值；再点同一天 = value 不变 → `change` 不触发 → 无法激活（"选不中"）。需先点别处再退回才行。
- **方案**：未激活时 `input.value` 保持**空字符串**，边界值仅由叠加层 `.drs-value` 显示（CSS 已透明化原生文字）。激活态由 `startActive/endActive` 依 `value !== ""` 判断，不再靠"值是否等于边界"推断。
- **改动**（`src/visual.ts`）：`onChange`（删"空值自动回填边界"，仅按非空设 active）、`updateValueDisplay`（未激活显示边界值、激活显示已选值）、`update()` 初始化（value 置空+active=false）、`resetToRange()`（value 置空+active=false）、`restoreFilter()`（先全置空+未激活再按 conditions 填值）。
- **已知小限制（已向用户说明）**：value 为空时浏览器日历默认落点（min 月或 max 月）取决于浏览器版本，需导入实测确认。核心修复（点任意日期必触发激活）不受影响；不引入 focus 临时填值增强（会重现"选不中"bug）。
- 已重新编译 `DateRangeSlicer20260822001.1.0.0.0.pbiviz`。

## DateRangeSlicer 回滚到"自动恢复边界"版（11:38 已编译）
- **背景**：用户反馈上一版"未激活 value 留空"改动"没啥效果"，要求回滚到它的**上一个版本** = "自动恢复边界"版（未激活侧 value 预填 min/max、显示边界值、清空自动恢复边界且不参与筛选）。
- **注意**：项目 git 仓库无提交（"No commits yet"），无法 git 回退，按对话记录反向改写 `src/visual.ts`。
- **改动**（`src/visual.ts`，与 11:30「value 留空」版反向）：
  - `onChange`：删 `value!==""` 隐式激活；改为——清空时 `value` 自动恢复为 `rangeMin/rangeMax` 且 `active=false`，非空则 `active=true`。
  - `updateValueDisplay`：直接按 `input.value`（已选日期或边界值）格式化显示，不再区分 active。
  - `update()` 初始化：`value` 置为 `rangeMin/rangeMax`+`active=false`。
  - `resetToRange()`/`restoreFilter()`：归位时 `value` 恢复边界值而非空串。
  - 全部 `toDateInput(rangeMin/rangeMax)` 加空值守卫，防无数据崩溃。
- **已知回归（用户已接受，待后续讨论）**：未激活侧 value 占住边界日期 → 点日历中同一边界日不再触发 change → 最小/最大日期需先选其它日期再改回才能激活。这正是当初改"value 留空"要修的问题。
- 已重新编译 `DateRangeSlicer20260822001.1.0.0.0.pbiviz`（dist + 工作区根目录副本均更新 11:38）。
- 提示：git 无提交记录，建议本次回滚后做一次 `git init/commit` 基线，便于后续真·版本回退。

## 项目 git 基线提交（11:51 已完成）
- 项目目录此前已是 git 仓库但无任何提交；本日补建 **`.gitignore` + 首次提交（root-commit `f44b2cb`）**，默认分支 `master`，仅本地、未 push（远程尚未创建）。
- `.gitignore` 决策：忽略 `.venv/ node_modules/ dist/ .tmp/ .vscode/ *.tmp_pbiviz.json`、`*.pbix`（用户要求报表不进库）、`PowerBI-visuals-ChicletSlicer/` 整目录（自带 .git 的嵌套仓库+第三方拷贝）、`visual/ChicletSlicer*.pbiviz`（第三方产物）、`s1/s1b/s2.json`（待删残留）。纳入：DateRangeSlicer 源码与配置、`PBI-Style-深蓝暗色.json`+校验脚本、项目级 skills、导入用 `visual/DateRangeSlicer20260822001.1.0.0.0.pbiviz` 交付物。
- 后续如需回滚：`git checkout f44b2cb -- <path>` 取历史文件；建远程后 `git remote add origin <url>` + `git push -u origin master` 即可上传已有本地提交。
- 注：`visual/DateRangeSlicer20260822001.1.0.0.0.pbiviz` 实际位于 `visual/` 下（非工作区根目录），导入 PBIRS 认此路径。

## 主题书签导航器选中态降噪（12:16 已完成）
- **问题**：`bookmarkNavigator.selected` 整片填充辅色紫 `#7F77DD` + 白字 bold，在 `#0A1428/#142436` 深蓝环境里过抢眼，导航控件抢数据焦点。
- **决策（方案A）**：让 bookmarkNavigator 与 pageNavigator 同规则——选中的底色用高亮面板 `#1A2D44`（不再整片填紫），强调色只做 1px 边框收口（`#378ADD`）；hover 态边框也收为 `show:false`（仅底色升一阶 `#1A2D44`）。文字 default `#B4B2A9` / hover `#FFFFFF` / selected `#FFFFFF` bold / disabled `#5F5E5A` 保持不变。
- **改动**（`PBI-Style-深蓝暗色.json`）：`bookmarkNavigator` 的 `selected.fill` `#7F77DD`→`#1A2D44`、`selected.outline` `#7F77DD`→`#378ADD`、`hover.outline` `#378ADD`→`show:false`。
- 已跑 `validate_theme.py`（schema 2.143）通过，0 错误。同步更新 `pbi-theme-design` skill 第5节选中态描述（bookmarkNavigator 不再整片填紫）。

## pbi-theme-design 技能补强 + 第 2 主题验证（13:25）
- **评估结论**：原技能能写"同结构换基色的暗色主题"，但缺①6 级阶梯具体推导法、②第 2 个完整范例、③强调色选法、④阶梯色/结构项区分、⑤浅色反转说明。
- **补强动作**（直接 Edit `营收概况\.workbuddy\skills\pbi-theme-design\SKILL.md`）：
  1. 第③节 step1 改为具体推导法：同色相 + 逐档提亮，L 序列 8→13→18→25→33→43，给出深蓝 L 值对照，要求页面底 vs 卡片 ≥1.5:1、卡片 vs 白字 ≥7:1。
  2. 新增「四、派生实战范例：深青暗色」：完整 6 级阶梯表（#05140F→#2A6450）、8 色板（主#2FBFA8/辅#6FD3C4）、强调色选法、必须随主题换的阶梯色清单。
  3. 第③节 step4 明确"仅 radius/border.show/title.show/gridlineStyle/labels 透明底白字/四态机 保留"，border.color/gridlineColor/shape.fill/导航器 fill outline 属阶梯必须换。
  4. 原「四、常见坑」→「五、常见坑」并补强：阶梯色 vs 结构项混淆、浅色未反转两条新坑。
- **可用性证明**：用该方法论生成 `PBI-Style-深青暗色.json`（青绿暗色），`validate_theme.py` 校验通过 0 错误。至此技能能产出≥2 套可复用主题模板。
- 注：新增的 `PBI-Style-深青暗色.json` 与 skill 改动均尚未 git 提交（基线 commit 之后新增/modified，untracked/modified）。

## 方向纠偏：暗色报告不符合阅读习惯 → 改白底范式（13:40）
- **用户反馈**：深青暗色主题"有点丑、不符合阅读习惯"，要求先明确"什么画布背景+视觉对象背景色符合数据可视化报告的阅读习惯"。
- **确立标准（报告阅读习惯 = 白底范式）**：画布极浅中性灰 `#F4F6F8` + 视觉对象纯白 `#FFFFFF` 卡片 + 1px 浅灰边框 `#E1E5EA`；文字三级反转（白→`#1F2733`/`#605E5C`/`#BFBFBF`）；网格线比边框更浅 `#EDEFF2`。暗色只适合大屏/监控，不适合营收分析类"报告"（刺眼、密集表可读性差、打印退化）。**导航壳可暗、报告主体应白**。
- **动作1**：删除被否定的 `PBI-Style-深青暗色.json`（用户明确不要）。
- **动作2**：新建 `PBI-Style-浅色.json`（白底范式完整主题）：画布 `#F4F6F8`/卡片 `#FFFFFF`/边框 `#E1E5EA`/高亮 `#EFF6FC`/分隔 `#EDEFF2`；dataColors 主 `#2E7DD1`、辅 `#7C6FD0`、涨绿 `#2FA572`、跌橙 `#E0603C`；导航器选中态极浅蓝底+主蓝 outline 收口。结构纪律（圆角8/边框显示/标题隐藏/虚线网格/四态机）沿用深蓝版。
- **动作3**：`validate_theme.py`（schema 2.143）校验 `PBI-Style-浅色.json` 通过，0 错误。
- **动作4**：同步更新 `pbi-theme-design` skill——① description 去掉"仅深色"限定；② 开头补"场景选型铁律（报告用白底、大屏用暗色）"；③ 原「四、深青暗色范例」替换为「四、白底范式标准 + 白底范例」（深青文件已删，范例改引 `PBI-Style-浅色.json`）。
  - 注：`PBI-Style-深蓝暗色.json` 保留未删（用户没要求删，可作对比/旧报表用）；以上新增/改动均未 git 提交。

## 导航器容器映射纠正 + shape 顶栏降噪（13:51）
- **用户纠偏**：顶部「结算收入/支付收入/项目/统计」是 **pageNavigator（页面导航器）**，不是书签导航器；卡片内「书签1/2」才是 **bookmarkNavigator**。我此前把容器映射写反了。
- **根因（丑）**：`PBI-Style-浅色.json` 里 pageNavigator 用白卡样式（白药丸）+ bookmarkNavigator 用深色样式（黑窟窿）；而实际摆位是 pageNavigator 在深色顶栏、bookmarkNavigator 在白卡 → 两处都错配。
- **修复**：把两套样式**对调**——pageNavigator 改深色幽灵标签（默认底=顶栏同色 `#1F2A37`、hover `#37474F`、selected `#455A64` 微提亮 + 白字 bold + 主蓝左条 `#2E7DD1` 3px，outline 全关）；bookmarkNavigator 改白卡样式（白底 `#FFFFFF` + 浅灰边 `#E1E5EA` + 中灰字 `#605E5C`，selected 极浅蓝 `#EFF6FC` + 主蓝边）；shape 顶栏填充 `#1F2733`→`#1F2A37`（去死黑）。`validate_theme.py` 校验通过 0 错误。
- **同步技能**：`pbi-theme-design` 第⑥节「导航器按容器配色」原写反（bookmark 深色/page 白卡）→ 改为正确映射（pageNavigator 在深色顶栏、bookmarkNavigator 在白卡）并加"同类型只能一套样式、布局别让导航器同时跨深浅容器"原则；第③节顶栏/导航壳行同步改 `#1F2A37`。

## 白底主题 shape 填充"发挥"修正（13:45）
- **用户反馈**：白底主题"平平无奇"，shape 填充色没发挥——根因是 shape.fill 默认填白 `#FFFFFF`，在白底上完全隐形，形状层毫无层次。
- **修正**（`PBI-Style-浅色.json`）：shape.fill `$id:default` `#FFFFFF`→`#A9CDED`（dataColors[2] 主色浅版，柔和品牌块）；shape.outline `$id:default` `show:false`→`show:true` + 浅灰边框 `#E1E5EA`。shape 拖出即为浅蓝品牌块（配浅灰边框），顶栏壳/卡片底/装饰块立刻有质感；想做强品牌页眉可手动把单个 shape 改主色 `#2E7DD1` + 白字。
- 重跑 `validate_theme.py` 通过 0 错误。同步更新 skill 第④节阶梯表"顶栏/导航壳"行：明确 shape 作壳时填品牌浅蓝/主色、勿填白隐形。

## 白底主题方案 B：深色顶栏（13:48）
- **用户反馈+决策**：上版浅蓝顶栏 `#A9CDED` 太"奶"、头部分层不清、整页缺深色锚点→ 选方案 B（深色高级顶栏 + 白底主体）。
- **改动**（`PBI-Style-浅色.json`）：shape.fill `$id:default` `#A9CDED`→`#1F2733`（近黑深灰，作顶栏壳）；shape.outline `$id:default` `show:true`→`show:false`（深色块无需浅灰边框）。导航按钮（pageNavigator/bookmarkNavigator）本就是白卡片 `#FFFFFF` + 主蓝 `#2E7DD1` 选中边框，符合方案 B，无需改。
- **实操提醒**：顶栏上的"营收分析"文本框需手动设白字、日期切片器在深色顶栏上需确认可见——主题管不到 shape 上的文本框色。
- 重跑 `validate_theme.py` 通过 0 错误。同步 skill 第④节：shape 作顶栏壳推荐深色 `#1F2733` + 白字（方案B）或主色 `#2E7DD1`（方案A），勿填白。

## 白底主题再修正：灰色画布 + 导航器按容器分色（13:52）
- **用户反馈**：方案 B 仍丑。明确新框架——画布**灰色** `#E6E9ED` + 卡片白 `#FFFFFF` + 形状（shape）专做**深色顶栏壳** `#1F2733`；**书签导航器嵌进深色顶栏**、**页面导航器放在白色卡片里**，两者容器底色不同，必须分别配色。
- **根因（丑的来源）**：之前把 pageNavigator / bookmarkNavigator 设成同一套白底样式，书签进深顶栏后"白块浮深栏"显突兀。
- **改动**（`PBI-Style-浅色.json`）：
  1. 画布 `page.background` `#F4F6F8`→`#E6E9ED`（更明确的灰，白卡浮起感增强）。
  2. `bookmarkNavigator` 全套改为**深色容器样式**：default/hover/disabled 底 `#1F2733`/`#2A3543`（融入深栏）+ 文字 `#C7CDD4`→`#FFFFFF`；selected 主蓝 `#2E7DD1` 填充 + 白字 bold + 白边 `#FFFFFF` 收口；无 accentBar。
  3. `pageNavigator` 维持**白色容器样式**（白底+浅灰边+主蓝选中边+辅紫左条），适配白卡。
  4. shape 仍 `#1F2733` 深色顶栏壳、outline 隐藏。
- 重跑 `validate_theme.py` 通过 0 错误。同步 skill 第④节：新增「6) 导航器按所在容器底色分别配色」关键规则（书签深栏/页面白卡两套），并改画布范例色 `#E6E9ED`。

## 形状填充色逻辑：报表类型决定结构色（13:55）
- **用户问题**：形状填充色的逻辑是什么？数据分析可视化分类是否对选色有帮助？
- **确立原则**：shape 在 Power BI 主题里主要承担"顶栏壳/导航壳/结构块"角色；其填充色**必须先按报表类型定**，不是凭感觉试色。
  - **分析型报表**（营收概况）：shape = 低饱和中性深灰，只做结构锚点、不抢数据焦点；拒绝高饱和品牌色（演示型）和死黑大屏壳（监控型）。
  - **大屏监控型**：shape = 深色壳，降低环境眩光。
  - **演示汇报型**：shape = 品牌强调色块，做视觉锤。
- **截图诊断**：`PBI-Style-浅色.json` 顶栏 `#1F2A37` 偏冷、偏死，像补丁；pageNavigator 选中态用灰块 `#455A64` 在深底上显脏。
- **修正**（`PBI-Style-浅色.json`）：
  1. `shape.fill` `$id:default` `#1F2A37` → `#2E3A45`（暖深灰，温润不冷、不补丁）。
  2. `pageNavigator`（深色顶栏容器）选中态：白底药丸 `#FFFFFF` + 深灰字 `#2E3A45` bold + 主蓝左条 accentBar `#2E7DD1` 3px；default/hover/disabled 底与顶栏同色融入；outline 全关。
  3. default/hover/disabled 文字同步从冷灰 `#B0BEC5` → 暖灰 `#C5CED6`，hover 白字。
- **效果预期**：顶部 pageNavigator 选中项像"白底胶囊"浮在暖深灰顶栏上，边界清晰、不脏、不抢下方数据。
- 重跑 `validate_theme.py` 通过 0 错误。
- **同步技能**：`pbi-theme-design` SKILL.md 第④节：① 顶栏/导航壳色范例 `#1F2A37`→`#2E3A45`；② 新增"结构色选择逻辑"说明；③ pageNavigator 深色容器选中态改为"白底药丸+主蓝左条"并解释为何不用灰块。

## 从参考图生成主题：FineReport 零售门户风（14:06）
- **触发**：用户给 FineReport 零售公司 Data Portal 截图，要求"根据这个图片生成主题"。
- **图片特征**：浅冷灰画布、浅蓝灰顶栏/侧边栏、纯白卡片、低饱和蓝色主调、浅绿辅助 KPI 卡、大量留白、圆角柔和。
- **派生步骤**：
  1. 判断类型：浅色门户/分析型报表 → 套用白底范式。
  2. 提取 6 级阶梯：画布 `#F3F6F9` → 顶栏壳 `#E4ECF3` → 卡片白 `#FFFFFF` → 高亮 `#EBF3FA` → 网格线 `#E8EDF2` → 边框 `#DCE3EA`。
  3. 提取品牌色：标题/KPI 深蓝 `#3B6CB4` 作主色，浅蓝 `#6A9BD5` 为辅色，浅绿 KPI `#5CB85C` 作正向色。
  4. 导航器配色：因截图顶部是**浅蓝灰条**（非深色壳），pageNavigator 改用浅色样式——default 与顶栏同色融入、selected 白底药丸 + 主蓝 outline/accentBar；bookmarkNavigator 白卡样式不变。
- **产出**：`PBI-Style-浅蓝FineReport.json`（新建）。
- 重跑 `validate_theme.py` 通过 0 错误。
- **同步技能**：`pbi-theme-design` SKILL.md 新增第⑤节「从参考图派生主题」，以本主题为范例，沉淀"看图→分类→提阶梯→提品牌色→按容器配导航器→命名"的完整流程。

## FineReport 主题方案 B：品牌蓝顶栏（14:12）
- **用户评审上一版**：浅蓝灰 `#E4ECF3` 顶栏太浅太平、pageNavigator 白底药丸对比弱、整页缺头部锚点 → 选方案 B（品牌蓝顶栏 + 白底主体）。
- **改动**（`PBI-Style-浅蓝FineReport.json`）：
  1. `shape.fill` `$id:default` `#E4ECF3` → `#3B6CB4`（品牌蓝顶栏壳）。
  2. `pageNavigator` 在蓝顶栏上用深色融入样式：default/hover/disabled 底 `#3B6CB4`/`#4A7BC0`/`#3B6CB4`（与顶栏同色），文字 default `#D6E2F0`、hover 白、disabled `#9DB4D0`（**不能用中灰字，蓝底上不可读**）；selected 维持白底药丸 `#FFFFFF` + 蓝字 `#3B6CB4` bold + 主蓝左条 accentBar `#3B6CB4`（白药丸在蓝底上对比最强）。
  3. `bookmarkNavigator`（白卡里）不变：白底 + 浅灰边 + 主蓝边选中。
- 重跑 `validate_theme.py` 通过 0 错误。
- **同步技能**：`pbi-theme-design` SKILL.md 第⑤节"4) 导航器按容器底色配"补充分品牌蓝/深色壳场景（文字用淡白、白药丸选中），并修正第⑤节落地文件描述顶栏色为 `#3B6CB4`。
- **实操提醒**：顶栏上"营收分析"文本框需手动设白字（主题管不到 shape 上的文本框）。

## FineReport 主题 shape 四态 + textbox 配置（14:30）
- **触发**：用户要求 shape 圆角 6、补全 default/hover/pressed/selected 四态，pageNavigator/bookmarkNavigator 四态统一，textbox 颜色与主题协调。
- **改动**（`PBI-Style-浅蓝FineReport.json`）：
  1. 全局卡片 `border.radius` 10 → **6**，与 shape 圆角统一。
  2. `shape` 扩展为完整四态：default `#3B6CB4`、hover `#4A7BC0`、pressed `#2F5A9E`、selected `#3B6CB4` + 白边 2px；文字四态均白；`border.radius` 6。
  3. `pageNavigator` 文字 default 调淡白 `#E8EEF7`、disabled `#8AA3C3`，在蓝顶栏上更清晰。
  4. `bookmarkNavigator` 补全 hover/disabled outline、selected 文字改 `#3B6CB4` bold，并加 selected 状态 **accentBar Left 3px `#3B6CB4`**。
  5. 新增 `textbox` 配置：透明底、无边框、标题隐藏、默认文本色 `#2C3E50`；**置于蓝色 shape 内的标题文本框需手动改白字**（主题无法按容器自动反白）。
- 重跑 `validate_theme.py` 通过 0 错误。
- **同步技能**：`pbi-theme-design` SKILL.md 更新 radius 为 6、补 shape 四态/textbox 说明、更新常见坑（shape 四态漏配、textbox 颜色上下文限制）。

## ⚠️ 纠错：shape 无四态（14:40）
- **用户指正**：「shape 没有 4 态，书签导航器和页面导航器才有 4 态」。经查 `reportThemeSchema-2.143.json` + 对照已验证主题 `PBI-Style-深蓝暗色.json` 确认：**shape 只有 `default` 单态**（外加一个 `{ "show": true }` 基对象），绝不可写 hover/pressed/selected；只有 `pageNavigator` / `bookmarkNavigator` 才有 default/hover/selected/disabled 四态 `$id`。
- **回滚**（`PBI-Style-浅蓝FineReport.json`）：把 shape 的 fill/outline 还原为仅 default 单态（fill `#3B6CB4`、outline 隐藏），删掉此前误加的 hover/pressed/selected。重跑 `validate_theme.py` 通过 0 错误。
- **pageNavigator 样式"没生效"排查**：本主题 pageNavigator 四态 `$id` 结构与参考主题逐字段一致、schema 合法 → **不是主题架构错误**。真正原因是 Power BI「覆盖坑」：导航器若在格式面板里手动改过（填充/轮廓/文字任一），该属性被锁定，主题不再覆盖。修复 = 在格式面板点各项「重置为默认值」或删除重加导航器后重导主题。另：非选中页签蓝底融进蓝色顶栏是有意设计（仅选中态白药丸浮出），若想更明显可调 default 为 `#4A7BC0` 或加浅描边。
- **同步技能**：`pbi-theme-design` SKILL.md 已删除两处错误「shape 四态」描述，改为明确规则「四态仅限 pageNavigator/bookmarkNavigator；shape 仅 default 单态」；并补「导航器格式被手动锁定导致主题不生效」到常见坑。

## ⚠️ 对话逻辑教训：改一个掉一个（14:45）
- **用户批评**：我上一轮只纠错 shape 四态，导航器的「边框(outline)+强调色(accentBar)」只补到 selected 一态，default/hover/disabled 以 `show:false` 糊弄，等于没按"四态×文本/填充/边框/强调"完整做。用户直言"丢三落四""对话逻辑有问题"，并明确"不完全理解需求就先找我确认"。
- **确认需求后落地**（`PBI-Style-浅蓝FineReport.json`）：用户确认"四态都描边、按状态选颜色；强调蓝条仅选中态"。把 `pageNavigator.outline` 的 default/hover/disabled 由 `show:false` 改为按状态色描边 —— default `#5A8BC8` / hover `#8AA3C3` / selected `#3B6CB4` / disabled `#2E568F`（weight 1）；`bookmarkNavigator` 本就四态描边（浅灰→主蓝选中）保持不变；`accentBar` 维持仅 `selected`（Left 3px `#3B6CB4`）。重跑 `validate_theme.py` 通过，0 错误。
- **流程改进**：今后改导航器/主题必须维护「文本 / 填充 / 边框 / 强调 × default/hover/selected/disabled × 各导航器」勾选清单，逐条核对后再回复；需求不确定先用 AskUserQuestion 确认，不再瞎猜。
- **覆盖坑再提醒**：若 PBIX 里手动改过导航器格式，主题不生效需「重置为默认值」或删视觉重加。

## pageNavigator 选中态强调条配色修正（14:54）
- **问题**：选中态是白底药丸、容器（形状顶栏）是蓝底 `#3B6CB4`；原强调条色也取 `#3B6CB4`，与紧邻的蓝色容器同色 → 视觉融入蓝栏、等于没效果（用户原话"直接重叠没有效果"）。
- **澄清**：用户确认「容器蓝底、选中白底」，强调条须按可视化指南选"有层级、舒适"的色——即与白底药丸、与蓝色容器都要拉开层次。
- **修正**（`PBI-Style-浅蓝FineReport.json`）：`pageNavigator.accentBar.selected.color` `#3B6CB4`→`#1F4E8C`（更深一阶的品牌蓝：白底上清晰、比容器蓝更深形成层级、同色系不刺眼）。`bookmarkNavigator` 在白卡内、选中药丸为浅蓝，蓝强调条不贴蓝容器，保持 `#3B6CB4` 不动。
- **设计铁律（记）**：导航器强调条/accentBar 颜色绝不能与所在容器底色同色；需按"与容器底色拉开层次 + 与对象底色对比"双条件选色。
- 重跑 `validate_theme.py` 通过，0 错误。

## 切片器标头白色不亮的根因（15:10）
- **用户现象**：切片器标头字体设为 `#FFFFFF` 但看着不亮、不像真白。
- **根因确认**：`PBI-Style-浅蓝FineReport.json` 的 `visualStyles` 此前**完全没有 `slicer / advancedSlicerVisual / textSlicer` 条目**，故标头白字不是主题给的 `#FFFFFF`，而是视觉对象自带偏灰白（自定义 DateRangeSlicer 标题默认带透明度 ≈`#F2F2F2`）；此外纯白小字压在中饱和蓝 `#3B6CB4` 上因抗锯齿+对比感知会显"软"一档（感知问题非 hex 错）。
- **修正**（`PBI-Style-浅蓝FineReport.json`）：新增 `slicer` / `advancedSlicerVisual` / `textSlicer` 三类型的 `header.fontColor=#FFFFFF` + Segoe UI 12px（仅设 header，不动 items 以免白底日期框内深色字被改白）。重跑 `validate_theme.py` 通过 0 错误。
- **仍偏灰的下一步**：若该切片器是自定义 DateRangeSlicer 视觉，主题对标题文本覆盖有限，须在 PBIX 格式窗格手动设标题色 `#FFFFFF`，或直接改 `visual.ts` 把标题色钉死 `#FFFFFF`（需确认实例类型）。
- 设计提示：自定义视觉标题/标头色常被视觉自身 CSS 的透明度或默认色覆盖，主题 JSON 不足以完全控制。

## 导航器样式精调：圆角 6、上下填充 5、书签选中态调整（15:20）
- **需求（用户）**：① 书签导航器选中态 `accentBar` 关闭；② 书签选中态 `outline` 改 `#3B6CB4`；③ 页面/书签导航器圆角改为 6（参考 shape 截图）；④ 两导航器常规属性填充上下间距改为 5。
- **Schema 依据**：`visual-pageNavigator`/`visual-bookmarkNavigator` 的 `*` 数组支持 `tileShape`/`rectangleRoundedCurve`/`cellPadding`/`top`/`bottom`；`border` 数组支持 `radius`。
- **改动**（`PBI-Style-浅蓝FineReport.json`）：
  1. `pageNavigator.*` 加 `top:5`、`bottom:5`、`tileShape:rectangleRoundedByPixel`、`rectangleRoundedCurve:6`；`border` 加 `radius:6`。
  2. `bookmarkNavigator.*` 同上，加 `top/bottom/tileShape/rectangleRoundedCurve`，`border` 加 `radius:6`。
  3. `bookmarkNavigator.outline.selected` 由 `show:false` → `show:true`，`lineColor:#3B6CB4`，`weight:1`。
  4. `bookmarkNavigator.accentBar.selected` 由 白色左条 → `show:false`。
- 重跑 `validate_theme.py`（schema 2.143）通过，0 错误。
- **注意**：`tileShape` 若被 PBIX 格式面板手动设置过，主题同样会被覆盖（覆盖坑）。如导入后按钮仍是 pill 形而非 6px 圆角矩形，需对导航器视觉「重置为默认值」或删视觉重加。

## 营收分析标题度量（15:35）
- **需求**：用度量值驱动"营收分析"标题，样式丰富且颜色符合 `PBI-Style-浅蓝FineReport.json` 主题。
- **方案**：用 **Card（新卡）** 视觉的三字段（Callout/Subtitle/Status）分别绑定 `_标题_主`(营收分析)/`_标题_英文`(REVENUE ANALYCIS)/`_标题_周期`(数据周期，随日期切片器变)；每个字段字体颜色用「条件格式→按字段」绑定对应主题色度量。
- **关键坑（同源教训）**：标题字色必须与所在容器对比——压在蓝顶栏 `#3B6CB4` 上时绝不能用主蓝当字色（会焊进背景），须用白 `#FFFFFF`/浅蓝 `#A4C7F0`/淡蓝灰 `#8AA3C3`；只有放白卡上才用主蓝 `#3B6CB4`/辅蓝 `#6A9BD5`/灰 `#5A6572`。
- **产物**：`营收概况\营收分析_标题度量.dax`（含三字段度量 + 三套色度量 + 备选单度量多行 `_标题_组合` + Card 新卡组装步骤）。
- **注**：左上角 "RA" 方块 logo 是独立 shape（填充 #3B6CB4+白字"RA"），非度量能生成。

## 深蓝暗色主题优化版（新建，15:45）
- **需求**：按 `PBI-Style-浅蓝FineReport.json` 的优化方式，给 `PBI-Style-深蓝暗色.json` 做一版优化，且**新建独立文件、不改动原文件**。
- **产出**：`PBI-Style-深蓝暗色-优化.json`（新建；原 `PBI-Style-深蓝暗色.json` 保留未动）。`validate_theme.py` 校验 0 错误。
- **套用的优化项**：
  1. **修 page.background 遗留 bug**：原深蓝版写 `"background":{"solid":...}` 导致页面底 `#0A1428` 静默失效；新版改为正确 `"color":{"solid":...}`。
  2. **导航器四态补全**：pageNavigator/bookmarkNavigator 的 `outline` 由"default/selected 两态"补全为 default/hover/selected/disabled 四态描边（默认态用分隔/边框色 `#1E3A5F`/`#2C4A6B`、hover/selected 用主蓝 `#378ADD`、disabled 用边框色）；bookmarkNavigator 显式补 `accentBar` 四态全 `show:false`。
  3. **圆角 6 + 上下填充 5**：全局卡片 `border.radius` 8→6；两导航器加 `tileShape=rectangleRoundedByPixel`/`rectangleRoundedCurve=6`/`top:5`/`bottom:5`/`border.radius:6`；shape `border.radius:6`。
  4. **切片器标头白色**：新增 `slicer`/`advancedSlicerVisual`/`textSlicer` 三类型 `header.fontColor=#FFFFFF`。
  5. **textbox 透明配置**：新增 `textbox`（透明底、标题隐藏、文本色 `#FFFFFF`）。
- **保留的深色美学**：选中态仍用高亮面板 `#1A2D44`（非白药丸，维持暗色调性）；accentBar 改用主蓝 `#378ADD` w4（原是紫 `#7F77DD` w3，统一为蓝系）；文字三级灰 `#B4B2A9`/`#FFFFFF`/`#5F5E5A` 不变。
- **注**：浅蓝版的"白药丸选中"思路未直接照抄（那会破坏暗色调性），改为在暗色阶梯内完成四态 + 圆角 + 填充的完整度；若用户后续想要暗色版也用白药丸选中，再单独调。
